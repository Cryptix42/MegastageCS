; *************************
; Project: MegastageCS
; File: Utility.lib.dasm
; *************************
:memory_copy
set push, a
set push, b
set push, c
set push, i
set i, 0xffff
sub b, 1
:mem_copy_loop
add i, 1
add b, 1
set [b], [a+i]
ifn i, c
set pc, mem_copy_loop
set i, pop
set c, pop
set b, pop
set a, pop
set pc, pop

:memory_fill

set pc, pop